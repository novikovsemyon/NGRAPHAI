<Window x:Class="NGraph.Views.SchemeIniEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NGraph — Редактор INI" Width="860" Height="660" MinWidth="620" MinHeight="460"
        Icon="/NGraph;component/Resources/Icons/NG_structura_32.png">
    <Window.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="CompactDialogStyles.xaml"/></ResourceDictionary.MergedDictionaries></ResourceDictionary></Window.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Обозначения из INI" Style="{StaticResource Heading}"/>
        <TextBox x:Name="FilePath" Grid.Row="1" IsReadOnly="True" Margin="0,0,0,10"/>
        <TextBox x:Name="Editor" Grid.Row="2" AcceptsReturn="True" AcceptsTab="True" TextWrapping="NoWrap" FontFamily="Consolas" FontSize="13"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" SpellCheck.IsEnabled="False"/>
        <StackPanel Grid.Row="3" Margin="0,10,0,10">
            <TextBlock Text="Секция [ADSK_Позиция], ключ Symbol — имя типоразмера УГО. После сохранения таблица и изображения обновятся." TextWrapping="Wrap"/>
            <TextBlock Text="Сохранение в UTF-16. Предыдущая версия останется рядом в файле .ini.bak." TextWrapping="Wrap" Foreground="#52627A" Margin="0,4,0,0"/>
            <TextBlock x:Name="Status" Foreground="#AD2234" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="Cancel_Click"/>
            <Button x:Name="Save" Content="Сохранить и закрыть" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
