<Window x:Class="NGraph.Views.HovsRevisionCompareWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="NGraph — Сравнение ХОВС" Width="1260" Height="720" MinWidth="920" MinHeight="540" FontSize="13" Icon="/NGraph;component/Resources/Icons/NG_hovs_32.png" Closed="Window_Closed">
  <Window.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="/NGraph;component/Views/CompactDialogStyles.xaml"/></ResourceDictionary.MergedDictionaries></ResourceDictionary></Window.Resources>
  <Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel><TextBlock x:Name="HeadingText" Style="{StaticResource Heading}"/><TextBlock Style="{StaticResource Body}" Text="Выберите две или больше ревизий. Столбцы расположены от старой к новой; сравниваются сохранённые данные."/></StackPanel>
    <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="250" MinWidth="180"/><ColumnDefinition Width="7"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <DockPanel><WrapPanel DockPanel.Dock="Top" Margin="0,0,0,6"><Button Content="Последние 2" Click="Latest_Click"/><Button Content="Все" Click="All_Click"/><Button Content="Снять" Click="None_Click"/></WrapPanel>
        <DataGrid x:Name="RevisionList" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns>
          <DataGridTemplateColumn Header="✓" Width="32"><DataGridTemplateColumn.CellTemplate><DataTemplate><CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
          <DataGridTemplateColumn Header="Ревизии" Width="*"><DataGridTemplateColumn.CellTemplate><DataTemplate><StackPanel Margin="4"><TextBlock Text="{Binding Revision.Name}" TextWrapping="Wrap"/><TextBlock Text="{Binding Revision.CreatedLocal}" Foreground="#52627A" FontSize="11"/></StackPanel></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
        </DataGrid.Columns></DataGrid>
      </DockPanel>
      <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#D5DDE9"/>
      <DockPanel Grid.Column="2"><Grid DockPanel.Dock="Top" Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
        <CheckBox x:Name="OnlyChanges" Content="Только изменения" IsChecked="True" Checked="Filter_Changed" Unchecked="Filter_Changed" VerticalAlignment="Center" Margin="0,0,12,0"/>
        <TextBox Grid.Column="1" x:Name="Search" TextChanged="Search_Changed" ToolTip="Поиск по установке, статусу, изменениям и значениям всех ревизий" AutomationProperties.Name="Поиск в сравнении"/>
      </Grid>
      <DataGrid x:Name="Comparison" AutoGenerateColumns="False" IsReadOnly="True" SelectionUnit="CellOrRowHeader" ClipboardCopyMode="IncludeHeader">
        <DataGrid.RowStyle><Style TargetType="DataGridRow"><Style.Triggers>
          <DataTrigger Binding="{Binding Status}" Value="Добавлена"><Setter Property="Background" Value="#E8F5E9"/></DataTrigger>
          <DataTrigger Binding="{Binding Status}" Value="Удалена"><Setter Property="Background" Value="#FDEBEC"/></DataTrigger>
          <DataTrigger Binding="{Binding Status}" Value="Изменена"><Setter Property="Background" Value="#FFF7DA"/></DataTrigger>
        </Style.Triggers></Style></DataGrid.RowStyle>
      </DataGrid></DockPanel>
    </Grid>
    <DockPanel Grid.Row="2" Margin="0,8,0,0"><Button DockPanel.Dock="Right" Content="Закрыть" Click="Close_Click"/><TextBlock x:Name="Summary" TextWrapping="Wrap" VerticalAlignment="Center"/></DockPanel>
  </Grid>
</Window>
